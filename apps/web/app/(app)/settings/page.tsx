import { SettingsPage } from '@/components/settings/SettingsPage';

/** Account settings route (auth-gated by the layout's AuthGate). */
export default function Settings() {
  return <SettingsPage />;
}
